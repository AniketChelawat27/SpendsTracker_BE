# Server
PORT=3001
NODE_ENV=development

# MongoDB – use MongoDB Compass or Atlas connection string
# Local: mongodb://localhost:27017/spend-tracker
# Atlas: mongodb+srv://user:pass@cluster.mongodb.net/spend-tracker
MONGODB_URI=mongodb://localhost:27017/spend-tracker

# JWT – for MongoDB auth (login/signup). Required when Firebase is not configured.
JWT_SECRET=your-secret-key-change-in-production

# Firebase Admin – for verifying frontend ID tokens (optional; JWT used when not set)
# Option A: path to service account JSON file
GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.json

# Option B: paste minified JSON as one line (e.g. for production)
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"..."}
